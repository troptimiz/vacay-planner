<div class="edit-form-container">
    <div class="row">
        <div class="col-md-12">
            <div class="item-container">
                <div class="data-form">
                    <div class="form-heading">
                        <h2>Update Phone Number</h2>
                    </div>
                    {{productPhone}}
                    <form role="form" id="update-phone-form">
                        <input type="hidden" name="id" value="{{productId}}"/>
                        <input type="hidden" name="phoneId" value="{{productPhone._id}}"/>
                        <div class="form-group">
                            <label for="">Contact Type:</label>
                            <input type="textbox" name="contactType" value="{{productPhone.contactType}}" class="form-control" placeholder="Contact Type"/>
                            <div class="clearfix"></div>
                        </div>
                        <div class="form-group">
                            <label for="">Contact Number:</label>
                            <input type="textbox" name="contactNumber" value="{{productPhone.contactNumber}}" class="form-control" placeholder="Contact Number"/>
                            <div class="clearfix"></div>
                        </div>
                        
                        <button type="button" class="btn btn-default" id="update-phone">Submit</button>
                        <button type="button" class="cancel-edit-category btn btn-default">Cancel</button>
                     </form>
                </div>                
            </div>
        </div>
    </div>
</div>
